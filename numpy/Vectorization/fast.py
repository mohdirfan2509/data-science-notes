# Vectorization means performing operations on entire arrays (vectors, matrices) at once, instead of using loops to process one element at a time.

import numpy as np

arr1=np.array([1,2,3,4])
arr2=np.array([5,6,7,8])
print(arr1+arr2)

print(3*arr1)